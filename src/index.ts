import { match } from './match';
import { Option, none, some } from './option';
import { Result, ok, error } from './result';

// Option
export { none, some };
export type { Option };

// Result
export { ok, error };
export type { Result };

// Match
export { match };
